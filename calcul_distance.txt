#include <math.h>

double dx = xB - xA;
double dy = yB - yA;

double t =
    ((xway - xA) * dx + (yway - yA) * dy)
    / (dx * dx + dy * dy);

double xp = xA + t * dx;
double yp = yA + t * dy;

double distance =
    sqrt(
        (xp - xA) * (xp - xA) +
        (yp - yA) * (yp - yA)
    );