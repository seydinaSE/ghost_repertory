double dx = xB - xA;
double dy = yB - yA;

double t =
    ((xway - xA) * dx + (yway - yA) * dy)
    / (dx * dx + dy * dy);

double xp = x1 + t * dx;
double yp = y1 + t * dy;


double distance =
    Sqrt(
        (xp - xA) * (xp - xA) +
        (yp - yA) * (yp - yA)
    );